pipeline {
    environment {
        MAVEN_LOCAL_REPO = '${WORKSPACE}/.m2/repository'
    }

    agent {
        docker {
            image 'maven:3.9.6-eclipse-temurin-21-alpine'
            args "-v /var/run/docker.sock:/var/run/docker.sock \
                  -v ${env.JENKINS_HOME}/test_assets/${env.JOB_NAME}/assets:${env.WORKSPACE}/backend/src/test/resources/assets \
                  -u 963:965 \
                  -e JENKINS_HOME=${env.JENKINS_HOME} -e JOB_NAME=${env.JOB_NAME} \
                  --env-file ${env.JENKINS_HOME}/test_assets/${env.JOB_NAME}/.env.dev-test \
                  --network host"
        }
    }

    stages {
        stage("Test") {
            steps {
                echo "Test"
                sh 'ls -la /var/lib/jenkins/workspace/test-xmr-backend_dev/backend/src/test/resources/assets'
                sh '''
                    cat > ${WORKSPACE}/backend/src/test/resources/assets/.env <<EOF
                    JENKINS_HOME=${JENKINS_HOME}
                    JOB_NAME=${JOB_NAME}
                    EOF
                '''
                sh 'cd backend && mvn -Dmaven.repo.local=${MAVEN_LOCAL_REPO} dependency:go-offline clean test'
            }
        }

        stage("Build") {
            when { expression { env.BRANCH_NAME == 'master' } }

            steps {
                echo "Build"
                sh 'java --version'
            }
        }

        stage("Deploy") {
            when { expression { env.BRANCH_NAME == 'master' } }

            steps {
                echo "Deploy"
            }
        }
    }
}